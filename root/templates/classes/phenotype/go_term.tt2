[% # This generic statement belongs at the start of every template %]
[% PROCESS identify_templates(my_component=component.name) IF c.config.debug_view %]

[% widget = c.stash.widget %]

[% widget = widget %]
 

[% WRAPPER $field_block  title="phenotype observed in the following " _ fields.go_term.data.keys.size _ " Go_term entries" key="go_term" %]
		    [% count = 0 %]
		    [% FOREACH obj IN fields.go_term.data.keys.sort %]
			[% count = count + 1 %]
			[% IF count == 16 %]
			      <div class="toggle">..more</div>
			      <div class="sequence-container">
			[% END %]
			[% tag2link(fields.go_term.data.$obj) %]
			[% UNLESS loop.last %]<br>[% END %]	
		    [% END %]
		    [% IF count >= 16 %]</div>[% END %]
[% END %]