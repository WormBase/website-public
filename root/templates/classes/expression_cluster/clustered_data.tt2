[%

WRAPPER $field_block title="Microarray" key="microarray";

    build_data_table (
		order = [ 
			'microarray',
			'experiment',
			'minimum',
			'maximum',
			],
		columns = {				 		
			microarray => 'Microarray',
			experiment => 'Experiment',
			minimum =>'Minimum',
			maximum =>'Maximum',
			},
		key = 'microarray');
END;


WRAPPER $field_block title="SAGE tags" key="sage_tags";
     link_objects(fields.sage_tag.data);
END;

%]