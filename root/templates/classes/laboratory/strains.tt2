[%

WRAPPER $field_block title="" key="strains";

   build_data_table(order=['strain','genotype'],
                    columns={ strain => 'Strain',
		              genotype   => 'Genotype'},
                    key='strains');

END;

%]