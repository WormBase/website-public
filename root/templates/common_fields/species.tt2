[%#

  Page    : *
  Widget  : *
  Section : Species
  Stash   : species

%]

[% # This generic statement belongs at the start of every template %]
[% PROCESS identify_templates(this_component=component.name) IF c.config.debug_view %]

[% # Don't display unless we have a genus and species %]

   [% IF c.stash.fields.taxonomy.data.genus %]
      [% WRAPPER "boilerplate/field" title="Taxonomy" key="taxonomy" %]
            <span class="species">[% c.stash.fields.taxonomy.data.genus %][% " " %][% c.stash.fields.taxonomy.data.species %]</span>
      [% END %]
   [% END %]
