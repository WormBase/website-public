      [% tag2link(item.name) %]
    </li>
    <span class="species">[% item.taxonomy.genus %] [% item.taxonomy.species %]</span>

[% desc = item.description.join() %]
<div class="text-min">
  [% markup(desc) %]
</div>
