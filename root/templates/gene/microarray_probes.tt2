[% # This generic statement belongs at the start of every template %]
[% PROCESS identify_templates(this_component=component.name) IF c.config.debug_view %]

[% WRAPPER "boilerplate/field" title="Microarray probes" %]
    [% FOR probe=microarray_probes %]
       [% url.Link(item=probe.0 text=join(probe.0, "[",probe.1,"]")) %]
    [% END %]
[% END %]
