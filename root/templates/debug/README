This directory contains special templates that are used
when in debug mode.  For example: the standard index pages
for each class are replaced with these that show all
widgets and fields for a given class.
