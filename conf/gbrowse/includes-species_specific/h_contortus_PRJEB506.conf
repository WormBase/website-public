[GENERAL]
description = H. contortus (current release)
database    = this_database
examples    = scaffold_9705 scaffold_9732:20..2000 CG42611
html1 = sub { return CGI::h1(CGI::i('Haemonchus contortus (BioProject: PRJEB506')); }

[this_database:database]
db_args     = -adaptor DBI::mysql
              -dsn dbi:mysql:database=h_contortus_PRJEB506_WS240;host=mysql.wormbase.org
	      -user wormbase
	      -pass sea3l3ganz

[PRIMARY_GENE_TRACK]
citation     = Gene predictions were produced by WormBase, using the 2007 version of 
               the Ensembl RNASeq gene-building pipeline.
               The purple and blue colors indicate transcripts on the forward and 
               reverse strands respectively. If sufficient room is available between 
               features, gene models end with a triangle; if not a small arrow is used.

